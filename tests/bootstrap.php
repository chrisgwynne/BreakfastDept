<?php

declare(strict_types=1);

/**
 * PHPUnit bootstrap. Loads the Composer autoloader (which registers the
 * Breakfast\Platform PSR-4 namespace and the Kirby core).
 */

require dirname(__DIR__) . '/vendor/autoload.php';
